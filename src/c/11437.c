#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

int main() {
    int n, m, a, b;
    int *arr;
    
    scanf("%d\n", &n);
    
    arr = (int*)malloc(sizeof(int) * n);
    for(int i = 0; i < n-1; i++){
        scanf("%d %d", &a, &b);
    }
    
    
    
}